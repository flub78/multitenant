{{=[[ ]]=}}
<!-- index.blade.php -->
@extends('master')

@section('content')

    @if (\Session::has('success'))
      <div class="alert alert-success">
        <p>{{ \Session::get('success') }}</p>
      </div><br />
     @endif
    <table class="table table-striped">
    <thead>
      
      <tr>
      	[[#list]]
      	<th>@lang('[[element]].[[name]]')</th>
      	[[/list]]
      
        <th colspan="2">@lang('general.action')</th>
      </tr>
    </thead>
    <tbody>
      
      @foreach($[[element]]s as $[[element]])
      <tr>
      	[[#list]]
      	<td>[[& field_display ]]</td>
      	[[/list]]
      	        
        <td><a href="{{action('[[class_name]]Controller@edit', $[[element]]['id'])}}" class="btn btn-warning">@lang('general.edit')</a></td>
        <td>
          <form action="{{action('[[class_name]]Controller@destroy', $[[element]]['id'])}}" method="post">
            @csrf
            <input name="_method" type="hidden" value="DELETE">
            <button class="btn btn-danger" type="submit">@lang('general.delete')</button>
          </form>
        </td>
      </tr>
      @endforeach
      
    </tbody>
  </table>
  
  <a href="{{url('[[table]]')}}/create"><button type="submit" class="btn btn-primary" >@lang('general.create') @lang('[[element]].element')</button></a> 
  </br>

@endsection