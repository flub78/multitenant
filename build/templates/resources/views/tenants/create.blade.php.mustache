{{=[[ ]]=}}
<!-- [[class_name]] create.blade.php -->

@php
use App\Helpers\BladeHelper as Blade;
@endphp

@extends('layouts.app')

@section('content')

<style>
  .uper {
    margin-top: 40px;
  }
</style>

<div class="card uper">
  <div class="card-header">
    {{__('[[element]].new')}}
  </div>
  <div class="card-body">
    @if ($errors->any())
      <div class="alert alert-danger">
        <ul>
            @foreach ($errors->all() as $error)
              <li>{{ $error }}</li>
            @endforeach
        </ul>
      </div><br />
    @endif
    
      <form method="post" action="{{ route('[[element]].store') }}">
           @csrf
           
           [[# form_field_list ]]
           <div class="form-group">
             [[&label]]
             [[&input_create]]
           </div>
           
           [[/ form_field_list ]]
           
           @button_submit({{__('general.submit')}})

      </form>
  </div>
</div>
@endsection